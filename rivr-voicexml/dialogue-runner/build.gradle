apply plugin: 'war'

publishing {
    publications {
        ivy(IvyPublication) { from components.web }
    }
}

artifacts { archives war }

uploadArchives {
    repositories {
        configure([mavenCentral, knox]) {
            pom.project { packaging 'war' }
        }
    }
}